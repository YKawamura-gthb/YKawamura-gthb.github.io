name: Update Google Scholar stats

on:
  workflow_dispatch:
  schedule:
    - cron: "17 5 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tools/update-scholar-stats.js
      - name: Commit updated stats
        run: |
          if git diff --quiet -- scholar-stats.json site-data.js; then
            echo "Scholar stats are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scholar-stats.json site-data.js
          git commit -m "Update Google Scholar stats"
          git push
